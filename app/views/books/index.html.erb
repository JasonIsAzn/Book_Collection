<h1>Home Screen</h1>
<table>
  <% @books.each do |book| %>
    <tr>
      <td><%= link_to book.title, book_path(book) %></td>
      <td><%= button_to 'Edit', edit_book_path(book), method: 'get'%></td>
    <% end %>
  </table>
